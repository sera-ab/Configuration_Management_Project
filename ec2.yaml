---
-name: vreate ec2 instance
hosts: localhost
tasks:
  - name: create instances
    ec2: 
      key_name: mykey
      instance_type: t2.micro
      image: ami_123456
      group: webserver
      count: 3
      vpc_subnet_id: subnet-29e63245
     assign_publi
